name: "BudUseCases: Build and Push Nightly Docker Image"

on:
  workflow_dispatch:
  push:
    paths:
      - 'services/budusecases/**'
    branches:
      - master

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./services/budusecases

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to DockerHub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./services/budusecases
          push: true
          file: ./services/budusecases/deploy/Dockerfile
          tags: budstudio/budusecases:nightly

import { ChatSettings } from "@/app/components/bud/chat/HistoryList";

export function addOrUpdateSettings(settings: ChatSettings, chatId: string){
//   return db.collection("settings").doc("settings").set(settings);
}
apiVersion: dapr.io/v1alpha1
kind: Configuration
metadata:
  name: appconfig
spec:
  tracing:
    samplingRate: "1"
    zipkin:
      endpointAddress: ""
  features:
    - name: ServiceInvocation
      enabled: true
    - name: StateStore
      enabled: true
    - name: PubSub
      enabled: true

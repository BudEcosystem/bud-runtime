# Service/Clients
dapr >= 1.13.0
budmicroframe @ git+https://github.com/BudEcosystem/bud-microframe.git

# API
fastapi >= 0.111.1
uvicorn >= 0.30.3
pydantic >= 2.8.2
pydantic-settings >= 2.3.4
python-multipart >= 0.0.6  # For file upload support

# Document Processing
docling >= 2.0.0  # Document OCR and VLM processing
docling-core >= 2.0.0  # Core docling functionality

# HTTP Clients
httpx >= 0.24.0  # Async HTTP client for VLM API calls
aiofiles >= 23.0.0  # Async file operations

# Utils
structlog >= 24.4.0
cachetools >= 5.4.0

# Database
alembic-postgresql-enum>=1.6.1
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-dev
spec:
  selector:
    matchLabels:
      app: {{ .Release.Name }}-dev
  replicas: 1
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-dev
      annotations:
        date: "{{ now }}"
    spec:
      containers:
      - name: dev
        image: docker.io/budstudio/status
        imagePullPolicy: "Always"
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-dev
spec:
  selector:
    app: {{ .Release.Name }}-dev
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
